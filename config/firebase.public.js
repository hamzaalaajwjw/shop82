export const firebaseConfig = {
  // removed (centralized in config/keys.js),
  // removed (centralized in config/keys.js),
  // removed (centralized in config/keys.js),
  // removed (centralized in config/keys.js),
  // removed (centralized in config/keys.js),
  // removed (centralized in config/keys.js)
};